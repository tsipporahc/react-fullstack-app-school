/* NotFound - Display a message letting the user know that the requested page can't be found. */

import React from 'react';

const NotFound = () => {
  return (
    <div>
      <h2>
        🚧 Sorry for the construction. The requested page can't be found. 🚧
      </h2>
    </div>
  );
};

export default NotFound;
